all:
  hosts:
    localhost:
      ansible_connection: local
  vars:
    vcenter_hostname: "192.168.1.22"
    vcenter_username: "Administrator@vsphere.local"
    vcenter_password: "P@$$w0rd!@^eSk!20!24"
    vcenter_datacenter: "eSkyIT"
    vcenter_cluster: "C3: DT & BK"